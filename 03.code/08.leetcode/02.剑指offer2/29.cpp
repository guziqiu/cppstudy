#include <iostream>
#include <cmath>
#include <cstdio>
#include <cstdlib>
#include <vector>
#include <stack>
#include <queue>
#include <string>
#include <map>
#include <set>
#include <algorithm>
using namespace std;
vector<int> spiralOrder(vector<vector<int> >& matrix) {
	

}

int main() {


	vector<vector<int> > matrix;
	vector<int> a({1, 2, 3});
	vector<int> b({4, 5, 6});
	vector<int> c({7, 8, 9});

	matrix.push_back(a);
	matrix.push_back(b);
	matrix.push_back(c);
	spiralOrder(matrix);


	return 0;
}
